/* USB identity of the ring when plugged in (wired mode). */
#define USB_IO_PID 0x0002
#define USB_IO_PRODUCT "Pointer Ring (wired)"
#define USB_IO_MAX_POWER_2MA 100 /* 200 mA: board + battery charging */
